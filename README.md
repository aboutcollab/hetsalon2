# Het Salon 2 — tenant page (generated)

This repo is a **deploy target only**. Every file here is built by
`make_tenant_offer.py` in the private `studio-about` repo and pushed by
`publish_tenant_page.sh`. Never edit anything here by hand — it will be
overwritten on the next publish.

Live: https://aboutcollab.github.io/hetsalon2/
